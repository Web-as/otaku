import React, { useState } from 'react';
// import { Button, Card, Input, Badge, Modal, Tabs, TabsList, TabsTrigger, TabsContent } from '@otaku/ui';

export const DesignSystemPreview = () => {
  // const [isModalOpen, setIsModalOpen] = useState(false);

  return (
    <div className="p-8 space-y-8 bg-gray-950 min-h-screen text-white">
      <h1 className="text-3xl font-bold mb-6">Design System Primitives</h1>
      <p className="text-gray-400">This component requires @otaku/ui package which is not currently installed.</p>
    </div>
  );
};
